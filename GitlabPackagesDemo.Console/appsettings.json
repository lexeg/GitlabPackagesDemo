{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "System": "Information",
            "Microsoft": "Information"
        }
    },
    "GitLabSettings": {
        "Host": "{{ gitlab_settings_host }}",
        "PrivateToken": "{{ gitlab_settings_private_token }}"
    },
    "SearchSettings": {
        "SearchText": "{{ search_settings_search_text }}",
        "FileExtension": "{{ search_settings_file_extension }}"
    }
}